server:
  port: 8080

logging:
  path: logs/
  file: mylog.log

spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/testhub?useSSL=false&useUnicode=true&characterEncoding=utf-8&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai
    username: root
    password: root123456

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
      - mapper/*
